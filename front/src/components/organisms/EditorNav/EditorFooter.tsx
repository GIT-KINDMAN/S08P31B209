import EditorNavButton from "@/components/molecules/EditorNavButton/EditorNavButton";

import { NavFooter } from "./EditorNav.styled";

const EditorFooter = () => {
  return (
    <NavFooter>
      <EditorNavButton></EditorNavButton>
    </NavFooter>
  );
};

export default EditorFooter;
